# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
python-dotenv>=1.0.0

# Agent graph
langgraph>=0.2.0

# Brand research (optional — skipped if TAVILY_API_KEY not set)
tavily-python>=0.3.0

# Tracing (optional — skipped if OVERMIND_API_KEY not set)
overmind>=0.1.0

# Emotion scoring — GoEmotions (RoBERTa) + CLIP
torch>=2.2.0
transformers>=4.40.0   # covers CLIPModel, CLIPProcessor, RoBERTa
numpy>=1.26.0
Pillow>=10.0.0         # image decoding for CLIP scorer
